package labpal;

import beepbeeptests.DadProcessor;
import netp.PacketSource;

public class DadExp extends StreamExperiment {
	
	public DadExp()
	{
		/*setProcessor(new DadProcessor());
		setSource(new PacketSource("ipv6.DAD.issue.pcap"));
		setNumEvents(9);*/
	}

}
